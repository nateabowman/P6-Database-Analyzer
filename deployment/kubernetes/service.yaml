apiVersion: v1
kind: Service
metadata:
  name: p6-analyzer-service
  labels:
    app: p6-analyzer
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: p6-analyzer

