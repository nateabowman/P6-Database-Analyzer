version: '3.8'

services:
  p6-analyzer:
    build: .
    container_name: p6-analyzer
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    environment:
      - LOG_LEVEL=INFO
      - LOG_JSON=false
    ports:
      - "8000:8000"
    restart: unless-stopped

